/* Declara una variable llamada contador y asígnale un valor inicial de
0. Incrementa el valor de contador en 1. Imprime el valor actual de contador. Vuelve a
incrementar el valor de contador en 1. Imprime nuevamente el valor actual de contador.
*/
let contador = 0;
contador = ++contador;
console.log(contador);
contador = ++contador;
console.log(contador);